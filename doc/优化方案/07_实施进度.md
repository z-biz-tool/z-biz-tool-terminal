# 07 · 实施进度（2026-09-22）

> 本文件记录 `doc/优化方案/` 从「设计方案」到实际落地的进度。
> 基线：文档 `0d23303`，后端能力合入 `6bbe098`。所有指标都是**实测值**，未跑通的部分显式标注为未验证。

---

## 7.1 阶段一 P0（6/6 已落地）

| 任务 | 落地位置 | 验证 |
| --- | --- | --- |
| T-1-1 主机密钥校验 / known_hosts | `src-tauri/src/hostkeys.rs`（TOFU 判定 `Trusted/Unknown/Changed`）、`ssh.rs` `verify_server_key`/`confirm_host_key`（oneshot + 120s 超时）、前端 `src/components/HostKeyPrompt.tsx`（监听 `host-key-verify`/`host-key-changed` → `ssh_resolve_host_key`） | 单测 8 项（hostkeys）绿；密钥变更**不提供**任何"仍然连接"旁路；`strict_host_key` 开关可回退 |
| T-1-2 诊断命令注入 | `commands.rs::validate_host`（只放行字母数字 `.` `-` `_` `:`，`[...]` 解 IPv6；**控制字符在 `trim()` 之前即拒绝**）、`ssh.rs::shell_escape` | 5 个 `validate_host` 测试（22 条注入载荷全拒 + 首尾 `\r\n\t\0` 全拒 + 合法名/完整 IPv6 全放行 + 空/超长 + 首尾空格仍容忍）与 2 个 `shell_escape` 测试（内部单引号必须成对包裹）；**`cargo test --lib` 43/43 绿** |
| T-1-3 全局 Mutex 头阻塞 | `ssh.rs`：`Arc<RwLock<HashMap<String, Arc<SshSession>>>>`，`get_session()` 只在读锁内克隆 `Arc` | 见 `commands.rs::get_session`；锁不跨 `await` |
| T-1-4 危险命令网关 | 规则与分级 `src/utils/commandGuard.ts`；手输/粘贴状态机 `src/utils/inputGuard.ts`；统一入口 `src/services/commandGate.ts`；确认框 `src/components/DangerConfirm.tsx`；接线：`serverStore.executeSnippet`、`serverStore.executeCommand`、`BatchExecModal`、`TerminalView` 键盘输入与右键粘贴、AI 填入 | `commandGuard` 65/65、`inputGuard` 51/51（esbuild+node 断言矩阵）；`dangerous_command_guard` 开关默认开、可回退 |
| T-1-5 凭证明文落盘 | `src-tauri/src/secret.rs`（AES-256-GCM，信封 `enc:v1:<base64>`，master key 0600）、`config.rs` 写时封套/读时解封、`harden_storage()` 一次性迁移 + 目录 0600/0700 | 单测 9 项（secret）+ 3 项（config 落盘/迁移/导出）绿；旧明文配置保持可读一个版本后自动升级 |
| T-1-6 AI Key 加密 + 日志脱敏 | AI Key 走同一封套；`src-tauri/src/redact.rs` 行缓冲脱敏（ANSI 先剥离、PEM 状态机）；`ssh.rs` 会话日志 sink 受 `session_logging`/`log_redaction` 控制并 0600 | 单测绿；`export_config` 不再写出解密后的凭证（含 `ai.api_key`） |

P0 六项全部取得运行证据（`cargo test --lib` 43/43、`npm test` 见 7.6）。

## 7.2 阶段二 AI 诚实化（8/8 已落地）

已完成（阶段二 8/8 条全部有落地）：

- **T-2-1 配置持久化**：`aiStore` 改为 `get_ai_config`/`save_ai_config` 双向 IPC，未加载完成前拒绝保存并 `console.error`，保存失败前端如实报错（`AIChatModal.handleSaveSettings`）。
- **T-2-2 之前的假状态**：删除只写 localStorage 的明文 `loadAIConfig/saveAIConfig` 与从不编译的 `services/unifiedAIClient.ts`、`services/types.ts`。
- **T-2-2 流式与可中断停止**：`aiClient.ts` 新增 `chatStream()` —— 按行重组 SSE/NDJSON（跨网络包拆分的事件不丢字、坏 JSON 与心跳行跳过、无换行收尾的尾包不丢），`AbortSignal` 透传给 `fetch`，四种 provider 各自的 `extractDelta`/`streamEndpoint`（gemini 改 `:streamGenerateContent?alt=sse`，ollama 走裸 JSON）。`AIChatModal` 改为逐帧合并 delta 后落 state、停止即真正断流且保留已产出文本。
- **T-2-3 命令解释 / 错误分析接入**：`terminalFeeds` 从"只写"扩成"读写"注册表（`selection()` / `recentOutput(n)` 由 `TerminalView` 提供），`App` 用 `openAiExplain/openAiError/openAiCode/openAiGit` 统一快捷键与工具栏按钮入口 —— 分析对象来自终端选区（错误分析在无选区时退化为最近 60 行屏幕输出）。此前五个 AI 面板在 `App` 里被硬编码成 `command=""`/`error=""`/`code=""`/`diff=""`/`task=""`，打开即空白。
- **T-2-4**：AI 自然语言转命令不"生成即落地"——只有用户点「填入终端（不自动执行）」才把文本写入命令行**且不带回车**；该行由用户自己回车触发网关，并按 AI 来源从 `confirm` 升级为 `block`（P-1）。`AICodeEditor` 的"更新代码"与 `AIMultiAgents` 的输出同样只走这条只填不执行的通道。
- **T-2-5 Cloud Agent mock 下线**：`CloudAgent.tsx` 重写为「AI 与终端数据（本机）」，去掉 `setTimeout` 假同步与"已同步到云端"提示，明确声明无云端能力，仅提供本机数据查看/删除。工具栏图标与 Tooltip 同步改名。
- **T-2-6 死代码删除**：`src/agent/`（AgentManager/AgentPanel/SQLEditor/QueryHistoryViewer）整目录移除，全仓无引用。
- **T-2-7 Markdown**：新增 `src/utils/markdown.tsx` 替换原来的逐行 `startsWith("```")` 伪解析（它会把结束围栏当新块起点，从而吞掉代码块之后的整段回复；列表还输出没有 `<ul>` 的裸 `<li>`）。新实现覆盖围栏配对/语言标注、有序与无序列表分组、标题、引用、段落、行内 `code` 与粗体，全部走 React 文本节点（不 `dangerouslySetInnerHTML`、不解析外链）。
- **T-2-8 文档口径**：`AI_FEATURES.md` 全文重写（此前 9 项里有 4 项描述不存在或未接线的行为，如"实时流式""多设备同步""自动执行"）；`README.md` 补安全设置项、`master.key`/`known_hosts` 路径、`npm test`；`00_总览` 的"设计方案，未实现"抬头改为指向本文。

- **`disconnectServer` 漏关分屏面板**：它按 `tab.serverId` 过滤 tab，而 `splitTab` 允许面板指向另一台服务器（`pane.serverId`）——"断开某服务器"会留下该服务器名下的面板会话。现按面板归属关闭：整 tab 的面板都属于它才关 tab，否则逐个 `closePane`。
- **重启恢复判定看过期字段**：`loadConfig` 用 `tab.serverId` 判断"这台机器还在不在"，混合分屏下会误判为不存在而跳过自动重连。现改为"任一面板的服务器仍配置存在就重连"。

## 7.3 阶段三 性能与稳定性（8/8 已落地）

已完成：

- **T-3-5 分屏重连只救第一个面板**：`reconnectTab` 旧实现先把**全部**面板标成 `connecting` 并清掉 `sessionId`，等 3 秒后只发起一条连接、只回填 `panes[0]`，其余面板**永久停在 connecting**（旧会话 id 已被抹掉，等于既没重连也没人清理）。现改为逐面板委托 `reconnectPane`（它本来就按 `pane.serverId` 连接，满足 P-3 会话隔离），并发 `Promise.all` 后由面板状态推导 tab 状态。
- **T-3-6 重连退避**：新增 `src/utils/reconnectPolicy.ts`（纯函数）——第 1 次立即、其后 2s→4s→8s… 封顶 30s，叠加 ≤25% 向上抖动，连续失败 8 次放弃并落在 error 态；参数可配。`reconnectPane` 维护进程内的 `tabId:paneId` 失败计数与排队定时器：成功清零、`closeTab`/`closePane` 时连同排队定时器一起摘除（否则已销毁的面板会被定时器重新连回来），退避期间面板被关掉则放弃本轮，连接成功但面板已消失则立刻断开这条无归属的新会话（不留野会话）。用户主动点"重连"走 `manual` 分支：不等待、计数重置。
- 验证：`npm test` 的 `reconnect-policy` 27 例（指数序列单调不减、任意次数不超上限、抖动只增不减、上限边界、key 前缀按 tab 隔离）。
- **T-3-1 后端 PTY 输出批处理**：`ssh.rs` 读循环加窗口合并 —— 默认 `pty_batch_window_ms = 16`（≈60fps），窗口内小块拼成一次 `emit`；攒到 `PTY_BATCH_MAX_BYTES = 64 KB` 立刻刷出不等窗口；ticker 用 `MissedTickBehavior::Delay`，卡顿时不补发积压 tick 放大延迟；Eof/ExitStatus 与循环任何出口都先刷尾巴，保证 `[会话已关闭]` 永远排在最后。置 `pty_batch_window_ms = 0` 即回退逐块下发（§5.7）。验证：`pty_batch_should_flush` 单测覆盖批处理开启下 `0 / 上限-1 / 刚好上限 / 4 倍上限` 四种长度，以及回退开关（窗口=0）下任何长度都必须刷出。
- **T-3-2 日志移出热路径**：会话日志的磁盘写从读循环搬进独立 task（`run_log_writer`），读循环只 `send`，慢盘不再拖住终端输出；通道深度 `LOG_CHANNEL_CAPACITY = 256`，关掉 `session_log_async` 时压到 1 —— 退化为"写完上一块才收下一块"，而不是另养一份同步实现。脱敏仍是跨块行缓冲（`log_writer_redacts_across_chunks_and_flushes_tail`）。
- **T-3-3 前端背压 + 单一分发**：新增 `src/services/ptyBus.ts`（全应用只 `listen("pty-output")` 一次、按 `session_id` 本地同步分发；N 个分屏不再各反序列化一次只为丢掉"不是我"；启动 PTY 前必须 await，否则首屏输出丢在监听就绪之前；监听起来失败不留 rejected promise）与 `src/utils/terminalWriter.ts`（回调驱动写入：同一时刻只在途一次 `term.write()`，两次回调之间到达的小块合并下发）。`BatchExecModal` 改走 `subscribeAllPtyOutput`。验证：`pty-pipeline` 10 例（取消订阅后槽位回收、缺字段载荷丢弃、先订阅后 await 不丢事件、一次 write 未回调不叠加第二次、合并不丢字节不乱序、dispose 不被迟到回调复活）。
- **T-3-4 execute 上限与超时**：`EXECUTE_OUTPUT_LIMIT = 2 MB`（超出即截断并标 `truncated`）、`EXECUTE_TIMEOUT_SECS = 60`；截断点改走字符边界对齐的 `append_capped`（见 7.5）。单测锁住"刚好填满不算截断 / 溢出必定截断 / 中文不断字节"。
- **T-3-8 forwards abort / 连接期取消**：`stop_forward` 摘表后 `handle.abort()`（否则 JoinHandle 要等底层 accept 出错才退出）；`disconnect()` 先 drain 掉全部转发任务再关通道；SFTP 子系统初始化包 `timeout(5s)`，失败回退 `ls -la` 而不拖住连接。前端侧的重连定时器摘除已随 T-3-6 落地。

- **T-3-7 WebGL 渲染（本轮补做）**：新增依赖 `@xterm/addon-webgl@0.18`（peer `@xterm/xterm ^5`，未动 xterm 版本）。装载策略放在 `src/utils/webglRenderer.ts`：这个模块**不 import** xterm 的 UMD 包（它依赖 `self`，node 里加载即炸），只接收调用方注入的 addon 工厂，于是"装不上/丢上下文必须退回 DOM 渲染"这条策略本身可在 node 断言。`TerminalView` 在 `term.open() + fit()` 之后装载，面板卸载时 `renderer.dispose()`。回退语义：装载期抛错 → 捕获 + 回收半初始化 addon + `console.warn`；运行中 `onContextLoss`（分屏多面板会各占一个 WebGL 上下文，超过浏览器上限时最老的那个会被强制 lose）→ dispose 让 xterm 换回 DomRenderer，并用一个 `dead` 标志保证"面板已销毁就不再报第二次回退"。开关 `webgl_renderer` 默认开、可关（§5.7）。**运行时未验证**：WKWebView 上是否真的建得起 WebGL2 上下文没实测过，但失败路径是自动回退，最坏情况等同今天的 DOM 渲染。代价：单 chunk 从 1,764.82 kB 涨到 1,867.33 kB（gzip 536.01 → 561.64 kB）。
- 验证：`npm test` 的 `webgl-renderer` 5 例（关闭时完全不构造 addon、成功装载后 dispose 只下发一次、activate 抛错回退且不把异常抛给初始化、丢上下文立刻 dispose 且只报一次原因、dispose 后迟到的丢失回调不复活）。

未完成：无（阶段三 8/8 落地，其中 T-3-7 属可选项且仅到"策略有单测 + 能编译"这一层）。

## 7.4 阶段四 测试与安全（T-4-1…T-4-7 全部落地，端到端仍缺真机）

- **T-4-1 前端测试脚手架**：`tests/run.mjs` + `package.json` 的 `test` 脚本（详见 7.6）。
- **T-4-3 CI 门禁**：新增 `.github/workflows/ci.yml`，与只负责出包的 `build.yml` 分开。前端 job：`npm ci` → `typecheck` → `npm test` → `build`；后端 job（macOS runner，免装 webkit 系统依赖）：`cargo fmt --check` → `cargo test --lib` → `clippy`（`continue-on-error`，存量 warning 未清零，暂不卡门禁）。**prettier 不入 CI**：HEAD 上有 35 个文件不符合 `.prettierrc.json`，全量格式化会产生上万行噪声 diff 淹没安全改动，留作独立 chore。
- **T-4-4 文件命令路径白名单**：新增 `src-tauri/src/paths.rs`。前端能触达本地文件的四个命令全部改走白名单：`read_file_content`、`read_file_as_base64`、`get_file_modified_time`、`open_file_with_default_app`（沿用 `config.rs::read_session_log` 的 canonicalize + `starts_with` 范式）。允许根只有**家目录**与**系统临时目录**（对话框选中的文件与 SFTP 下载落盘都落在这两处），`..` 直接拒、指向根外的符号链接由 canonicalize 拆穿；`open_file_with_default_app` 允许目录（"打开配置目录"），其余三个只允许文件。Windows 分支从 `cmd /c start <path>` 换成 `explorer.exe <path>`，路径不再二次过 shell 解析。新增 `save_key_file(path, content)`：写盘固定 0600、缺失父目录按 0700 创建，`KeyGenModal` 由"前端 plugin-fs 直接写"改为调这条命令。
- **T-4-5 CSP + capabilities 收敛**：`capabilities/default.json` 从 `core:default + 4 个插件 default` 收敛为 `core:default` + `dialog:allow-message/open/save` + `clipboard-manager:allow-write-text`，**`fs:*` 与 `shell:*` 全部摘除**（前端已无一处 import 这两个插件）。`tauri.conf.json` 的 `security.csp` 从 `null` 改为分指令白名单：`default-src/script-src 'self'`（Tauri 自动注入 script nonce）、`style-src 'self' 'unsafe-inline'`（antd v6 的 CSS-in-JS 靠 JS 注入 `<style>` 节点，按 CSP 语义需要 `'unsafe-inline'`；此项**未经运行时实测**）、`connect-src 'self' ipc: http://ipc.localhost https: http://localhost:* ws://localhost:*`（IPC 协议 + 四家 provider 的 https + Ollama 本机 + dev HMR）、`object-src/frame-src 'none'`、`base-uri 'self'`、`form-action 'none'`。
- **T-4-6 导出脱敏复核**：已复核到位（T-1-6 时一并覆盖）——`export_config` 走"不写出解密后凭证"的分支，含 `ai.api_key`，由 `config::tests::export_never_writes_plaintext_credentials` 锁住；本轮补上"导出这件事本身进审计轨迹"（`{path, include_secrets, success, error}`，不含内容）。
- **T-4-7 审计日志（全链路已落地）**：
  - 后端新增 `src-tauri/src/audit.rs`：JSONL 追加写 `get_config_dir()/audit.log`，落盘 0600；递归脱敏复用 `redact::redact_once`，单个字符串 512 字符截断并标 `…[截断]`，单行超 `AUDIT_MAX_LINE_BYTES = 8 KB` 落成骨架行，文件超 `AUDIT_MAX_BYTES = 2 MB` 单代滚动到 `audit.log.1`；写失败只 `eprintln!`——**审计永远不能让用户操作失败**。
  - 记录点：`ssh_connect` / `ssh_connect_via_jump`（host/port/username/auth_type/success/error，**不含 password 与私钥**）、`ssh_disconnect`、`ssh_execute`、`export_config`、`save_key_file`（只记路径/字节数/结果），以及主机密钥的每一条判定（`auto_accepted` / `user_trusted` / `user_rejected` / `rejected_changed`（带期望与实际指纹）/ `rejected_no_ui` / `confirm_timeout` / `confirm_channel_closed` / `known_hosts_write_failed`）。
  - 判定分工：后端只记**它才知道的事实**（主机、用户、成功与否、输出字节数），不重判危险等级（单一真源仍是前端 `commandGuard`）；前端 `src/services/auditLog.ts` + `commandGate.decideCommand()` 补上后端无从知道的部分（等级、AI 升级、用户批准/拒绝、网关被关）。危险命令的**放行与拒绝都记**——只记放行那一半就成了一份自我表扬的执行清单；网关关闭记 `command_gate_bypassed`，AI 填入记 `ai_command_suggested`（带 `guard_level` 与是否真的填进去）。
  - `ssh_execute(session_id, command, gate)` 新增 `GateVerdict { confirmed, guard_level, source, guard_enabled }`；缺 `gate` 记 `gate_missing`、网关关闭记 `guard_disabled`，旁路在轨迹里看得见。`confirmed` 只在"确实弹过框"时非 null。
  - 设置页新增「审计日志」tab：刷新最近 100 条（中文动作标签 + 主机/命令摘要 + 已放行/已拒绝/成功/失败结论）、导出 JSON 或 CSV。CSV 做注入防护：`= + - @ \t \r` 起手的单元格前缀 `'`、换行折成空格、引号成对翻倍、带 UTF-8 BOM 让 Excel 认编码。查询/导出走 T-4-4 的路径白名单，导出动作自身再记一条。
  - 验证：后端 `audit` 7 例（单行 JSON 带 `ts`/`action`、嵌套 detail 递归脱敏、detail 里的换行不伪造额外行、文件 0600、超 2 MB 单代滚动、倒序查询且导出自身留痕、白名单外路径拒）+ 前端 `audit-gate` 6 例（安全命令零审计噪声且 `confirmed === null`、confirm 级在没挂确认框时 fail-closed 且带主机清单、AI 来源 confirm→block、block 级照记、网关关闭记 `guard_enabled: false`、审计投递失败不改变判定）。
  - **有意偏离 04 §4.9 的三点**（不假装完成）：① `ssh_pty_write` 不做逐键审计（按键热路径，代价与收益不对等）；② `dangerous_command_guard` 关闭时手输路径整个绕过 `LineInputGuard`，因此 `command_gate_bypassed` 只覆盖 snippet/批量/AI/`ssh_execute` 四条路径；③ 哈希链防篡改未实现（文档里列为可选）。

- T-4-2 覆盖面：网关规则、输入状态机、AI 流式/中断、Markdown 渲染、重连退避、PTY 输出管线、审计网关共 7 个测试文件；后端 `validate_host`/`shell_escape`/secret 封套/known_hosts/脱敏/导出/**路径白名单**/**审计落盘与导出**/**批处理与截断边界**各有单测。**SFTP 与 PTY 端到端仍无测试**（需要真机 SSH，属 T-4-2 剩余项）。

已知限制（本次收敛换来的，写清楚）：
- 从 U 盘 / 挂载卷（macOS `/Volumes/...`）直接 Zmodem 上传会被白名单拒（提示"路径不在允许的目录内"），需要先复制到用户目录；放开只需在 `paths.rs::allowed_roots()` 增一项。
- AI 自建网关如果是**局域网明文 http**（如 `http://192.168.x.x:8000`），会被 `connect-src` 拦掉；https 与 `localhost` 不受影响。回退方式是在 `tauri.conf.json` 的 `connect-src` 追加该来源或临时整体置 `null`。
- CSP 与 capabilities 只验证到"**能编译 + 权限标识符被 build script 接受**"这一层，`tauri dev` 仍未跑起来，弹窗与样式相关行为属运行时未验证项。

## 7.5 顺手修掉的既有缺陷（非计划条目）

- `src/index.css` 引入的 `./shared-style.css` 在仓库里根本不存在，导致 HEAD 上 `npm run build`/`npm run dev` 直接失败 —— 已补入 token 文件。
- 前端 typecheck 在 HEAD 有 17 个错误、且 `useAIStore`/`useTerminalAIStore` 等命名与 `types/ai.ts` 互相不匹配 —— 现 **0 错误**，`vite build` 通过。
- `services/aiClient.ts`：Claude 走 `/messages`+`anthropic-version`、Gemini 的 Key 从 URL query 移到 `x-goog-api-key` 头（避免落进网关访问日志，P-4）、Ollama 走 `/api/chat` 且参数放进 `options`。
- **`Ctrl+Shift+E` 死代码**：SFTP 面板与"AI 命令解释"抢同一条快捷键，且 SFTP 分支先 `return` —— 命令解释分支永远走不到。现已把解释改到 `Ctrl+Shift+X`（工具栏 Tooltip 同步）。
- **`ChatModal` 的"停止"按钮点不动**：`disabled={!input.trim()}` 在生成中（输入框已清空）恒为 true；且 `handleStop` 只改前端状态、不断请求。现两处都修：按钮生成中始终可点，停止真正 `abort()`。
- **`AICodeEditor` 改代码无效**：分析用的是外部 `code` 属性而非编辑区 `editCode`，且"应用建议"只弹一句 toast 不改内容 —— 现按编辑区分析、应用建议写回编辑区。
- **`AIGitCommit` 的两个假动作**：模型没给主标题时兜底成 `feat: 添加新功能`（与 diff 无关的编造），"使用此提交信息"回调到 `App` 的 `() => {}`（点了无反应）—— 现改为无标题即报错、"使用"= 复制标题+正文。
- **`custom` provider 会请求 `undefined/chat/completions`**：`baseUrl` 为空时不再拼空值，直接报"请先填写 Base URL"。切换 provider 时同步换 `baseUrl`/`model` 预设（`withProvider`），用户手填的非默认值保留 —— 此前从 openai 切到 claude 会带着 openai 的 URL 和模型名过去。
- **聊天历史无上限**：`z-terminal:ai-chat-history` 每轮整段重写且不截断，现限 200 条；系统提示不再写进历史（此前会逐轮堆叠）。
- **三处"看起来有、运行时被权限拒"的功能**（读 `tauri-plugin-*` 的 `permissions/default.toml` 才发现，不是靠跑出来的）：
  - `clipboard-manager:default` 的授权集是**空**（插件明说剪贴板默认不开），而 `SftpPanel` 复制路径调 `writeText` → 每次必然抛错、静默回退到 `navigator.clipboard`。现已显式授予 `clipboard-manager:allow-write-text`。
  - `fs:default` 只覆盖 App 专属目录，`KeyGenModal` 往 `$HOME/.ssh/` 写私钥、以及"另存为"选中的任意路径都在范围外 → 私钥保存功能在 HEAD 上是坏的。现改为后端 `save_key_file` 落盘（并顺带修掉"私钥按默认 0644 写出"的问题）。
  - `shell:default` 只有 `allow-open`，且 `open()` 要先过正则 `^((mailto:\w+)|(tel:\w+)|(https?://\w+)).+` → 设置页"打开配置目录"传的 `~/.z-terminal` 必然被拒（且 `~` 本来也不会展开）。现改走 `open_file_with_default_app` + 绝对路径。
- **`open_file_with_default_app` 在 Windows 上二次过 shell**：`cmd /c start <path>` 会把路径里的 `&`、引号再解析一次；换成 `explorer.exe <path>` 单参数传递。
- **远程输出截断会 panic 在 UTF-8 中间**：`collect_channel_output` 原按字节下标 `&chunk[..remaining]` 卡 2 MB 上限，而远程日志常见中文（3 字节/字），下标落在字符中间会在 SSH 读任务里直接 panic 掉整条会话 —— 新增 `append_capped` 把截断点回退到字符边界，并补 1 例单测（`汉字测试` 在 limit 5/4 下分别保留 `ab汉` / `ab`，且结果必须 `is_char_boundary`）。
- **审计测试与配置测试抢同一个进程级环境变量**：`audit.rs` 的测试切 `Z_TERMINAL_CONFIG_DIR` 时，`config.rs` 的落盘测试会读到别人的目录（真实红过一次：`config.json 应存在: NotFound` + 2 个 `PoisonError` 级联）。现改为 crate 级共用一把 `CONFIG_DIR_TEST_GUARD` 串行锁（`config::lock_config_dir_env()`），panic 时取 `into_inner()` 而不把 panic 外溢成其它测试的毒化失败。
- **`ptyBus` 监听起来失败会把 rejected promise 永久留在模块里**：一次 `listen()` 失败后，后续所有订阅都建立在同一个已拒绝的 promise 上、永久失败。现失败即清空 `starting` 并抛出，下次订阅可重试。

## 7.6 当前验证口径与已知缺口

- 前端：`npx tsc --noEmit` 0 错误；`npm run build` 通过（单 chunk 1,874.78 kB / gzip 564.20 kB，拆分仍是待办）。
- **新增依赖（本轮唯一一项）**：`@xterm/addon-webgl@^0.18.0`，peer 要求 `@xterm/xterm ^5`，与现有 `^5.5.0` 兼容，未改动 xterm 版本；`package.json`/`package-lock.json` 随之更新，体积代价见 T-3-7。
- **`npm test`（T-4-1 已落地）**：`tests/run.mjs` 用 vite 自带的 esbuild 把 `tests/*.test.ts(x)` 打包成 ESM 交 node 跑，按 `PASS n / FAIL m` 汇总并作为退出码。当前 **10 文件 / 271 例全绿**：`command-guard` 65、`input-guard` 51、`hostkeys` 37、`environment` 32、`reconnect-policy` 27、`ai-stream` 23、`markdown` 14、`pty-pipeline` 10、`audit-gate` 7、`webgl-renderer` 5。除 webgl addon 外零新增依赖；`audit-gate`、`pty-pipeline` 与 `webgl-renderer` 靠一个假的 `window.__TAURI_INTERNALS__`（`transformCallback` + `invoke`）、localStorage 打桩和 addon 替身，把 antd/`commandGate`/`serverStore` 直接拉进 node 跑。
- **CI（T-4-3 已落地）**：`.github/workflows/ci.yml` **已在 GitHub 上真实跑绿**：`c5e6b81`（3m2s）与 `063edd1` 两次 run 的 `frontend`/`backend` 两个 job 均 `success`（前端 typecheck + `npm test` + build，后端 `cargo fmt --check` + `cargo test --lib`，clippy 仍 non-gating）。本地等价命令同样全绿：`npm run typecheck`、`npm test`、`npm run build`、`cargo fmt --all --check`、`cargo test --lib`。clippy 在 CI 里是 `continue-on-error`（实测 `cargo clippy --lib` 17 条 warning，集中在 `redact.rs`/`secret.rs` 等存量文件的风格类提示，未清零）。**prettier 不入 CI**：HEAD 上有 35 个文件不符合 `.prettierrc.json`，全量格式化会产生上万行噪声 diff 淹没安全改动，留作独立 chore。
- 后端：`cargo test --lib` **71 passed / 0 failed**（基线 `6bbe098` 36 例 → 71）。按模块实测分布：`redact` 10、`secret` 9、`paths` 9、`commands` 9、`hostkeys` 14、`config` 8、`audit` 7、`ssh` 5。`cargo check --all-targets` 只剩 1 个存量 warning（`ForwardedChannel` 的字段未被读）；本轮审计读回 `host`/`username` 后，原先那批 dead-code warning 已消失。曾出现一次真实失败：`validate_host("ok\r")` 被 `trim()` 放过 —— 说明"拒注入"依赖每个调用方都记得再 trim 一次，已改成在 trim 之前拒绝任何控制字符（`commands.rs:36`），并补 2 例锁住该性质。
- 运行时 GUI 未验证：主机密钥弹窗、设置页新增的「审计日志」tab 都只有静态与单元层面证据，`tauri dev` 未跑起来；审计文件的真实滚动形态（超 2 MB 换代）与脱敏命中也没在真机上取过样；**WebGL 渲染器是否能在 WKWebView 里建起上下文同样未实测**（只验证了"建不起来会自动回退"这条策略）。「主机信任」tab（§7.8）与危险命令弹窗 + 环境标识（§7.8、§7.9）已在浏览器里用 stub `invoke` 跑过真实渲染与交互，但那仍不是 Tauri 运行时，后端 IPC 未对过真账号。
- `import_config` 导入他机密文凭证时按设计无法解密，需用户重填。
- 网关是"防误操作"级别的前端防线，远端主机的真实权限边界仍在服务端；后端不重判危险等级，只如实记下前端网关给它的判定（`gate.confirmed` / `gate_missing` / `guard_disabled`），旁路因此在审计轨迹里看得见（T-4-7）。

## 7.7 阶段五（可选增强）与总收口

阶段一~四已全部取得落地与测试证据：P0 6/6、阶段二 8/8、阶段三 8/8（含本轮补做的 T-3-7 WebGL）、阶段四 T-4-1…T-4-7 全落地。阶段五按 05 §阶段五 的"可选"定位**未开工**，本轮不假装完成：

| 任务 | 状态 | 备注 |
| --- | --- | --- |
| T-5-1 Profile 管理（外观/日志/重连/危险严格度预设） | 未开工 | 需要先把 `settings` 分组，否则预设之间会互相覆盖 |
| T-5-2 prod 环境视觉区分 | 已落地 | 见 §7.9 |
| T-5-3 known_hosts 管理 UI | 已落地 | 见 §7.8 |
| T-5-4 广播输入（默认关闭） | 未开工 | 必须叠加 P-2 逐主机确认，不能一键把危险命令发给 N 台 |
| T-5-5 命令历史检索 | 未开工 | |
| T-5-6 本地终端后端评估 | 未立项 | 03 §3.2 的结论仍是"暂不引入" |

另外：`00_总览` 阶段五里与审计并列提到的"审计导出"已随 T-4-7 落地（CSV/JSON 走 T-4-4 路径白名单），不必再等阶段五。

## 7.8 阶段五首轮：T-5-3 known_hosts 管理（提交 `c5e6b81` + 本轮前端）

后端（`src-tauri/src/hostkeys.rs`）：

- `known_hosts_list` 返回 `HostKeyView{hostSpec, host, port, algo, fingerprint, weakAlgo}`，指纹走 `entry_fingerprint()`，与首连弹窗、密钥变更告警同一口径（有测试钉住"视图指纹 == 连接时算出的指纹"）。
- `known_hosts_remove(host_spec)` 取代原先未校验、且"删不到也返回成功"的 `config::remove_host_key`：主机标识先过 `valid_host_spec`（空白/控制字符/`# @ \ " '`/超长一律拒），删 0 条如实报错，成功则写 `known_hosts_revoke` 审计。
- 顺手修掉一个真实的信任管理缺陷：`record_at` 原来按主机名整条覆盖，一台主机同时持有 rsa + ed25519 时，两次协商算法不同就会反复弹"首次确认"，把用户训练成无视指纹告警。现按 (host, algo) 覆盖，`remove` 仍按主机整条撤销（界面确认框如实写明会带走几条）。

前端：

- `src/utils/hostkeys.ts`：`hostSpec()` / `splitHostSpec()` 与 Rust 逐条对齐（只有 `[host]:port` 才带端口，端口 0/非数字/超范围按无端口），聚合排序用 code-unit 比较而不是 `localeCompare`（ICU 会忽略方括号标点，同一份文件在不同环境下顺序会跳）。
- 设置页新增「主机信任」tab：列表按主机聚合（多算法折叠成一行 + `共 N 台主机 / M 条记录`），搜索覆盖主机/端口/算法/指纹，指纹可一键复制，弱算法（`ssh-rsa`/`ssh-dss`）标橙色风险，命中已配置服务器时带上服务器名。撤销走 Popconfirm，明确列出"会一并删除 ssh-ed25519、ssh-rsa 共 2 条"以及"下次连接会重新要求确认指纹"。
- 审计动作表补 `known_hosts_revoke`，撤销记录在「审计日志」tab 里显示为中文而不是原始动作名。

实测：

- `cargo fmt --all --check` 通过；`cargo test --lib` **70 passed / 0 failed**（其中 `hostkeys` 14 例）。
- `npx tsc --noEmit` 0 错误；`npm test` **9 文件 / 238 例全绿**（新增 `hostkeys` 37 例）；`npm run build` 通过（1,872.48 kB / gzip 563.36 kB）。
- 浏览器实测（一次性 `.verify` harness，用 stub `invoke` 渲染真实 `SettingsModal`，验完即删）：tab 列表 8 项齐全；3 条记录聚合成 2 行且弱算法/服务器名/端口标签正确；搜索 `10.0.0`、`rsa`、无命中词分别得到预期行/空态；撤销 Popconfirm 文案含"共 2 条记录"，确认后 `known_hosts_remove` 收到 `{"hostSpec":"[10.0.0.1]:2222"}`、行消失、计数变为 `共 1 台主机 / 1 条记录`、toast 出现；把 `invoke` 改成抛错后显示"读取信任列表失败: boom"而不是伪装成"尚未信任任何主机"（并据此把计数行改为错误态下不展示，避免留下过期数字）。
- 仍是 **Tauri 运行时未验证**：`tauri dev` 未跑，真实 known_hosts 文件与真实弹窗未对过样。
## 7.9 阶段五次轮：T-5-2 prod 环境视觉区分（提交 `8a35e63` + 本轮）

设计取舍：**宁可少标，不可错标**。误标一台生产机会把红框变成噪声，用户从此无视它；漏标只是少一层提示。所以环境值是自由文本还是 `prod/production/线上/预发/qa/...` 都归一化（`src/utils/environment.ts` 的别名表），认不出的一律按"未标注"处理并且**不占任何像素**，不做任何猜测。

- 数据：`ServerConfig.environment?: string`（前端）+ `#[serde(default)] pub environment: Option<String>`（`src-tauri/src/config.rs`）。后端只原样存取、不参与判定，归一化集中在前端一处；存量配置缺字段读出来即 `None`，属 §5.7 的向后兼容读法，无需迁移。
- 侧栏：`ServerList` 名称后挂 `EnvBadge`（PROD 实心红 / STG 橙 / DEV 绿），编辑表单在"名称"下方提供带色块的环境下拉，搜索同时命中原始值与中文标签。
- 标签页：`App.tsx` 只在 **prod**  tab 上显示 `PROD` 小徽标，并给该 tab 的内容区加 `inset 0 3px 0 0 #d4380d` 顶边；预发/开发不进标签条，避免挤掉可读性。
- 弹窗：`DangerConfirm` 的主机清单逐条带 `[生产环境] / [预发环境]` 前缀（prod 加粗标红），并在告警里给出 `清单中有 N 台生产环境主机` —— P-2 的"受影响主机清单"因此在看得到颜色的地方也有，事后审计里也能复原（`commandGate.hostList()` 写进 `detail.hosts`）。

实测（当前口径见 §7.6）：

- `npx tsc --noEmit` 0 错误；`npm test` **10 文件 / 271 例全绿**（新增 `environment` 32 例，`audit-gate` 6 → 7 例钉住审计主机串带环境前缀）；`npm run build` 通过（1,874.78 kB / gzip 564.20 kB）。
- `cargo fmt --all --check` 通过；`cargo test --lib` **71 passed / 0 failed**（`config` 7 → 8，新增 environment 原样往返 + 缺字段读成 `None`）。
- 浏览器实测（一次性 `.verify` harness 渲染真实组件，验完即删）：
  - `EnvBadge`：prod 底色 `rgb(212,56,13)`、staging `rgb(212,136,6)`、dev `rgb(56,158,13)` 三色互不相同；`compact` 33×13 vs 常规 38×14；`undefined` 与乱填的 `sandbox` 都渲染出 **0 个 DOM 节点**。
  - 危险命令弹窗（真实 `confirmDangerousCommand("rm -rf ./build", 3 台混合主机)` 入口）：标题 `⚠️ 危险命令确认`、告警"递归强制删除"、`将影响 3 个会话` + `清单中有 1 台生产环境主机`，三行分别显示 `[生产环境] 订单主库 10.0.0.9:22`（前缀 `<strong>` 且 `color: rgb(212,56,13)`）、`[预发环境] 联调机 10.0.0.10:2222`、`未标注机 10.0.0.11:22`（无前缀）。
  - 整棵 `App` 在 stub `__TAURI_INTERNALS__`（`invoke`/`transformCallback`）下无哨兵渲染成功：3 个 tab 里只有 prod 那个的标签是 `订单主库PROD`（PROD 徽标数 = 1），红顶边只出现在 prod 内容区（`box-shadow: rgb(212,56,13) 0px 3px 0px 0px inset`），切到预发/未标注 tab 时该节点变 `display:none` 且没有任何新节点带上边框 —— 顶边跟着活动 tab 走，不会漏染。
- 两处踩到又修正的探针假象（记录以免下次重复误判）：① `DangerConfirmHost` 的 `enqueue` 在 `useEffect` 里注册，同 tick 调 `confirmDangerousCommand` 会按"无法确认"直接拒绝（这正是 P-2 的 fail-closed 行为，不是 bug），harness 必须等 commit 之后再调；② antd v6 的 Modal 结构是 `.ant-modal-container`（不再有 `.ant-modal-body`），且浏览器会把 `inset` 序列化到 `box-shadow` 末尾，按 `style` 属性字符串匹配 `inset 0 3px` 会得到假阴性 —— 一律改用 `getComputedStyle`。
- 仍是 **Tauri 运行时未验证**：`tauri dev` 未跑，真实配置落盘/真实 SSH 会话下的观感没取过样。
